echo "web-      1.)burpsuite(Windows and Linux)
                Burp Suite Professional is one of the most popular penetration testing and vulnerability finder tools, and is often used for checking web application security.

                2.)dirsearch(Windows and Linux)
                Dirsearch is a tool written in Python used to brute-force hidden web directories and files

                3.)hashpump(Linux)
                Hashpump is a tool that implements attacks against multiple hashing functions with OpenSSL, and supports extended attacks on lengths such as MD5, etc.

                4.)mitmproxy(Windows and Linux)
                Mitmproxy is an open source proxy application that allows intercepting HTTP and HTTPS connections between any HTTP(S) client.

                5.)net-creds(Windows and Linux)
                Net Creds is a free tool that sniffs passwords and hashes from a network interface.

                6.)sqlmap(Windows and Linux)
                SQL Injection is a code injection technique where an attacker executes malicious SQL queries that control a web application's database."
                
